-- Show that `sum [x] = x` for any number `x`.

Let `x` be a number, then:

      sum [x]
  =             { applying sum }
      x + sum []
  =             { applying sum }
      x + 0
  =             { applying + }
      x
